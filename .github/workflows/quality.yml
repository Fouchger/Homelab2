name: Quality

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint and test
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.28"
          enable-cache: true

      - name: Synchronize locked environment
        run: uv sync --locked --all-groups

      - name: Check formatting
        run: uv run --locked ruff format --check .

      - name: Run static checks
        run: uv run --locked ruff check .

      - name: Run tests
        run: uv run --locked pytest

      - name: Validate example configuration
        run: uv run --locked homelabctl validate --config config/examples/site.yaml

      - name: Verify generated schema is current
        run: |
          uv run --locked homelabctl schema --output config/schema/site.schema.json
          git diff --exit-code -- config/schema/site.schema.json

