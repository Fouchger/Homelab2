# Override the default config/sites/local.yaml path when required.
HOMELAB_CONFIG=config/sites/local.yaml

# Never commit the real token secret. SOPS integration will replace this
# environment variable in a later deployment phase.
PROXMOX_VE_API_TOKEN=replace-at-runtime

