# Override the default config/sites/local.yaml path when required.
HOMELAB_CONFIG=config/sites/local.yaml

# Override the default SOPS-encrypted runtime secret path when required.
# Token values belong inside this encrypted file, never in .env files.
HOMELAB_SECRETS=config/secrets/local.enc.yaml
